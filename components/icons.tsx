"use client";

import { Soccer, FileText, Layers, Box, MessageSquare, Image } from "lucide-react";

export const Icons = {
  all: FileText,
  component: Layers,
  form: Box,
  media: Image,
  discussion: MessageSquare,
  soccer: Soccer,
};